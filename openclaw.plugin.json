{
  "id": "calendar-sync",
  "name": "Calendar Sync",
  "description": "多源日历同步（企微 + iCloud），本地缓存 + cron 后台同步，查询毫秒级响应，支持企微→iCloud 单向镜像",
  "skills": ["."],
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {}
  }
}
